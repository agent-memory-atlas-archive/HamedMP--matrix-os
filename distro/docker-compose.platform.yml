# Production multi-tenant platform
# Usage: docker compose -f distro/docker-compose.platform.yml up -d
#
# Prerequisites:
#   - Cloudflare tunnel created: cloudflared tunnel create matrix-os
#   - Credentials at /etc/cloudflared/credentials.json
#   - DNS: CNAME *.matrix-os.com -> <tunnel-id>.cfargotunnel.com
#     (covers app.matrix-os.com and code.matrix-os.com)
#   - DNS: CNAME api.matrix-os.com -> <tunnel-id>.cfargotunnel.com
#   - DNS: CNAME matrix.matrix-os.com -> <tunnel-id>.cfargotunnel.com
#
# User containers are created dynamically by the platform orchestrator
# via dockerode -- they are NOT defined in this compose file.

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    command:
      - postgres
      - -c
      - max_connections=200
      - -c
      - shared_buffers=128MB
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: matrixos
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-matrixos}
      POSTGRES_DB: matrixos_platform
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U matrixos"]
      interval: 10s
      timeout: 5s
      start_period: 10s
    networks:
      - matrixos-net

  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel run
    volumes:
      - ./cloudflared.yml:/etc/cloudflared/config.yml:ro
      - cloudflared-creds:/etc/cloudflared
    depends_on:
      platform:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - matrixos-net

  cloudflared-watchdog:
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
        - NEXT_PUBLIC_CLERK_SIGN_IN_URL=${NEXT_PUBLIC_CLERK_SIGN_IN_URL:-/sign-in}
        - NEXT_PUBLIC_CLERK_SIGN_UP_URL=${NEXT_PUBLIC_CLERK_SIGN_UP_URL:-/sign-up}
        - NEXT_PUBLIC_POSTHOG_KEY=${NEXT_PUBLIC_POSTHOG_KEY:-}
        - NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=${NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN:-}
        - NEXT_PUBLIC_POSTHOG_HOST=${NEXT_PUBLIC_POSTHOG_HOST:-}
        - NEXT_PUBLIC_POSTHOG_API_HOST=${NEXT_PUBLIC_POSTHOG_API_HOST:-}
    entrypoint: ["node"]
    command: ["scripts/cloudflared-ws-watchdog.mjs"]
    environment:
      - PLATFORM_INTERNAL_URL=http://platform:9000
      - PLATFORM_PUBLIC_URL=${PLATFORM_PUBLIC_URL:-https://app.matrix-os.com}
      - PLATFORM_SECRET=${PLATFORM_SECRET}
      - PLATFORM_JWT_SECRET=${PLATFORM_JWT_SECRET}
      - CLOUDFLARED_WATCHDOG_INTERVAL_MS=${CLOUDFLARED_WATCHDOG_INTERVAL_MS:-60000}
      - CLOUDFLARED_WATCHDOG_PROBE_TIMEOUT_MS=${CLOUDFLARED_WATCHDOG_PROBE_TIMEOUT_MS:-10000}
      - CLOUDFLARED_WATCHDOG_FAILURE_THRESHOLD=${CLOUDFLARED_WATCHDOG_FAILURE_THRESHOLD:-3}
      - CLOUDFLARED_WATCHDOG_RESTART_COOLDOWN_MS=${CLOUDFLARED_WATCHDOG_RESTART_COOLDOWN_MS:-300000}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      platform:
        condition: service_healthy
      cloudflared:
        condition: service_started
    restart: unless-stopped
    networks:
      - matrixos-net

  platform:
    # Keep the Compose service tag independent from PLATFORM_IMAGE. The latter
    # may point the legacy local orchestrator at an operator-supplied image.
    image: matrixos-user:local
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
        - NEXT_PUBLIC_CLERK_SIGN_IN_URL=${NEXT_PUBLIC_CLERK_SIGN_IN_URL:-/sign-in}
        - NEXT_PUBLIC_CLERK_SIGN_UP_URL=${NEXT_PUBLIC_CLERK_SIGN_UP_URL:-/sign-up}
        - NEXT_PUBLIC_POSTHOG_KEY=${NEXT_PUBLIC_POSTHOG_KEY:-}
        - NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=${NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN:-}
        - NEXT_PUBLIC_POSTHOG_HOST=${NEXT_PUBLIC_POSTHOG_HOST:-}
        - NEXT_PUBLIC_POSTHOG_API_HOST=${NEXT_PUBLIC_POSTHOG_API_HOST:-}
    entrypoint: ["node", "--import=tsx"]
    command: ["packages/platform/src/main.ts"]
    environment:
      - PLATFORM_PORT=9000
      - PLATFORM_PUBLIC_URL=${PLATFORM_PUBLIC_URL:-}
      - PLATFORM_DATABASE_URL=postgresql://matrixos:${POSTGRES_PASSWORD:-matrixos}@postgres:5432/matrixos_platform
      - PLATFORM_SECRET=${PLATFORM_SECRET}
      - PLATFORM_JWT_SECRET=${PLATFORM_JWT_SECRET}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - POSTGRES_URL=postgresql://matrixos:${POSTGRES_PASSWORD:-matrixos}@postgres:5432
      - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
      - NEXT_PUBLIC_CLERK_SIGN_IN_URL=${NEXT_PUBLIC_CLERK_SIGN_IN_URL:-/sign-in}
      - NEXT_PUBLIC_CLERK_SIGN_UP_URL=${NEXT_PUBLIC_CLERK_SIGN_UP_URL:-/sign-up}
      - POSTHOG_TOKEN=${POSTHOG_TOKEN:-}
      - POSTHOG_HOST=${POSTHOG_HOST:-}
      - NEXT_PUBLIC_POSTHOG_KEY=${NEXT_PUBLIC_POSTHOG_KEY:-}
      - NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=${NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN:-}
      - NEXT_PUBLIC_POSTHOG_HOST=${NEXT_PUBLIC_POSTHOG_HOST:-}
      - NEXT_PUBLIC_POSTHOG_API_HOST=${NEXT_PUBLIC_POSTHOG_API_HOST:-}
      - NEXT_PUBLIC_MATRIX_APP_URL=${NEXT_PUBLIC_MATRIX_APP_URL:-}
      - MATRIX_APP_DOMAIN_HOSTS=${MATRIX_APP_DOMAIN_HOSTS:-}
      - MATRIX_BILLING_PROVIDER=${MATRIX_BILLING_PROVIDER:-}
      - MATRIX_STRIPE_BILLING_ENABLED=${MATRIX_STRIPE_BILLING_ENABLED:-false}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY:-}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET:-}
      - STRIPE_PRICE_MATRIX_STARTER_MONTHLY=${STRIPE_PRICE_MATRIX_STARTER_MONTHLY:-}
      - STRIPE_PRICE_MATRIX_STARTER_ANNUAL=${STRIPE_PRICE_MATRIX_STARTER_ANNUAL:-}
      - STRIPE_PRICE_MATRIX_BUILDER_MONTHLY=${STRIPE_PRICE_MATRIX_BUILDER_MONTHLY:-}
      - STRIPE_PRICE_MATRIX_BUILDER_ANNUAL=${STRIPE_PRICE_MATRIX_BUILDER_ANNUAL:-}
      - STRIPE_PRICE_MATRIX_MAX_MONTHLY=${STRIPE_PRICE_MATRIX_MAX_MONTHLY:-}
      - STRIPE_PRICE_MATRIX_MAX_ANNUAL=${STRIPE_PRICE_MATRIX_MAX_ANNUAL:-}
      - STRIPE_CHECKOUT_SUCCESS_URL=${STRIPE_CHECKOUT_SUCCESS_URL:-}
      - STRIPE_CHECKOUT_CANCEL_URL=${STRIPE_CHECKOUT_CANCEL_URL:-}
      - STRIPE_PORTAL_RETURN_URL=${STRIPE_PORTAL_RETURN_URL:-}
      - PIPEDREAM_CLIENT_ID=${PIPEDREAM_CLIENT_ID:-}
      - PIPEDREAM_CLIENT_SECRET=${PIPEDREAM_CLIENT_SECRET:-}
      - PIPEDREAM_PROJECT_ID=${PIPEDREAM_PROJECT_ID:-}
      - PIPEDREAM_ENVIRONMENT=${PIPEDREAM_ENVIRONMENT:-development}
      - PIPEDREAM_WEBHOOK_SECRET=${PIPEDREAM_WEBHOOK_SECRET:-}
      - PLATFORM_IMAGE=${PLATFORM_IMAGE:-matrixos-user:local}
      - AUTH_SHELL_PORT=3200
      - AUTH_SHELL_HOST=auth-shell
      # Customer VPS (spec 070)
      - CUSTOMER_VPS_ENABLED=${CUSTOMER_VPS_ENABLED:-false}
      - CUSTOMER_VPS_TLS_VERIFY=${CUSTOMER_VPS_TLS_VERIFY:-false}
      - CUSTOMER_VPS_IMAGE_VERSION=${CUSTOMER_VPS_IMAGE_VERSION:-stable}
      - HETZNER_API_TOKEN=${HETZNER_API_TOKEN:-}
      - HETZNER_LOCATION=${HETZNER_LOCATION:-fsn1}
      - HETZNER_SERVER_TYPE=${HETZNER_SERVER_TYPE:-cpx22}
      - HETZNER_IMAGE=${HETZNER_IMAGE:-ubuntu-24.04}
      - HETZNER_SSH_KEY_NAME=${HETZNER_SSH_KEY_NAME:-}
      - R2_ACCOUNT_ID=${R2_ACCOUNT_ID:-}
      - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID:-}
      - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY:-}
      - R2_BUCKET=${R2_BUCKET:-matrixos-sync}
      - R2_PREFIX_ROOT=${R2_PREFIX_ROOT:-matrixos-sync}
    ports:
      - "9000:9000"
    volumes:
      - platform-data:/data
      - /var/run/docker.sock:/var/run/docker.sock
      - user-data:/data/users
    depends_on:
      postgres:
        condition: service_healthy
      auth-shell:
        condition: service_healthy
    healthcheck:
      test: wget -qO- http://localhost:9000/health || exit 1
      interval: 10s
      timeout: 3s
      start_period: 5s
    restart: unless-stopped
    networks:
      - matrixos-net

  auth-shell:
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
        - NEXT_PUBLIC_CLERK_SIGN_IN_URL=${NEXT_PUBLIC_CLERK_SIGN_IN_URL:-/sign-in}
        - NEXT_PUBLIC_CLERK_SIGN_UP_URL=${NEXT_PUBLIC_CLERK_SIGN_UP_URL:-/sign-up}
        - NEXT_PUBLIC_POSTHOG_KEY=${NEXT_PUBLIC_POSTHOG_KEY:-}
        - NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=${NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN:-}
        - NEXT_PUBLIC_POSTHOG_HOST=${NEXT_PUBLIC_POSTHOG_HOST:-}
        - NEXT_PUBLIC_POSTHOG_API_HOST=${NEXT_PUBLIC_POSTHOG_API_HOST:-}
    entrypoint: ["node", "node_modules/next/dist/bin/next", "start", "shell", "-p", "3200"]
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY}
      - CLERK_SIGN_IN_URL=/sign-in
      - CLERK_SIGN_UP_URL=/sign-up
      - CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
      - CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/
      - POSTHOG_TOKEN=${POSTHOG_TOKEN:-}
      - POSTHOG_HOST=${POSTHOG_HOST:-}
      - NEXT_PUBLIC_POSTHOG_KEY=${NEXT_PUBLIC_POSTHOG_KEY:-}
      - NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=${NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN:-}
      - NEXT_PUBLIC_POSTHOG_HOST=${NEXT_PUBLIC_POSTHOG_HOST:-}
      - NEXT_PUBLIC_POSTHOG_API_HOST=${NEXT_PUBLIC_POSTHOG_API_HOST:-}
    healthcheck:
      test: wget -qO- http://localhost:3200/sign-in || exit 1
      interval: 10s
      timeout: 3s
      start_period: 15s
    restart: unless-stopped
    networks:
      - matrixos-net

  proxy:
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-}
        - NEXT_PUBLIC_CLERK_SIGN_IN_URL=${NEXT_PUBLIC_CLERK_SIGN_IN_URL:-/sign-in}
        - NEXT_PUBLIC_CLERK_SIGN_UP_URL=${NEXT_PUBLIC_CLERK_SIGN_UP_URL:-/sign-up}
    entrypoint: ["node", "--import=tsx"]
    command: ["packages/proxy/src/main.ts"]
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - PROXY_PORT=8080
      - PROXY_DB_PATH=/data/proxy.db
      - POSTHOG_TOKEN=${POSTHOG_TOKEN:-}
      - POSTHOG_HOST=${POSTHOG_HOST:-}
    ports:
      - "8080:8080"
    volumes:
      - proxy-data:/data
    healthcheck:
      test: wget -qO- http://localhost:8080/health || exit 1
      interval: 10s
      timeout: 3s
      start_period: 5s
    restart: unless-stopped
    networks:
      - matrixos-net

volumes:
  pgdata:
  cloudflared-creds:
  platform-data:
  proxy-data:
  user-data:

networks:
  matrixos-net:
    name: matrixos-net
